# ── Database ─────────────────────────────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_NAME=result_viewer
DB_USER=root
DB_PASS=changeme

# ── PDF Storage ──────────────────────────────────────────
PDF_ROOT=/data/pdfs

# ── Optional API-Key ─────────────────────────────────────
# If set, every /api request must include header X-API-Key
# API_KEY=my-secret-key

# ── Frontend (only needed when running outside Docker) ───
VITE_API_BASE_URL=http://localhost:8000
